plugins {
	id 'java'
}

allprojects {
	group = 'com.ohho'
	version = '0.0.1-SNAPSHOT'
	sourceCompatibility = '11'

	repositories {
		mavenCentral()
	}

	tasks.withType(Test) {
		useJUnitPlatform()
	}
}

subprojects {
	apply plugin: "java"
}

tasks.getByName('jar') {
	enabled = false
}
